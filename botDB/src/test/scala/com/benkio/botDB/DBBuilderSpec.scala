package com.benkio.botDB

import munit._

class DBBuilderSpec extends FunSuite {

  test("DBBuilder flattenResources should flat directories recursively in a list of files") {
    ???
  }

  test("DBBuilder populateMediaTable should call the databaseRepository for data insertion") { ??? }

  test("DBBuilder build should run the migrations and populate the DB") {
    ???
  }
}
