package com.benkio.chattelegramadapter.http.telegramreply.callbackreply

import munit.FunSuite

class PaginationSpec extends FunSuite {
  test("Pagination.reply should update inline keyboard markup with requested page") {
    assert(false)
  }

  test("TelegramCallbackReply.reply should delegate NextPage and PreviousPage callbacks to pagination") {
    assert(false)
  }

  test("TelegramCallbackReply.reply should delegate Media callback payload to media reply flow") {
    assert(false)
  }
}
